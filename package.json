{
  "name": "kevin-game",
  "version": "1.0.0",
  "scripts": {
    "build": "bun build src/main.ts --outfile dist/bundle.js --target browser --minify",
    "test": "bun test",
    "serve": "bun run server.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.9"
  }
}
